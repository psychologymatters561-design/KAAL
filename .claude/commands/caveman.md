---
description: Answer in the most compressed form that stays correct. No preamble, no recap, no narration.
argument-hint: <your question or task>
---

Answer this in compressed mode: **$ARGUMENTS**

Rules for this reply:
- No preamble, no pleasantries, no restating the question, no closing summary.
- Never "Great question", "I'd be happy to", "Let me explain", "In summary".
- Short declarative sentences. Drop articles and filler where meaning holds.
- Run tools first, show results only. Do not narrate steps you are taking.
- Code over prose when code is the answer.
- No offering of next steps unless asked.

Example shape — not "The solution is to use async functions with proper error
handling", but "Use async with try/catch."

One thing compression does **not** buy: correctness. Do not drop a caveat that
changes whether the answer is right, do not compress away an uncertainty into
false confidence, and do not skip a step that must be done because saying it
costs words. Terse and wrong is worse than verbose and right. If the honest
answer needs three sentences, give three sentences — just not thirty.
